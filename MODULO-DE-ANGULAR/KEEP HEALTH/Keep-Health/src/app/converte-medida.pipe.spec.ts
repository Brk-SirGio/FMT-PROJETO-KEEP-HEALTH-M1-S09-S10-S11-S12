import { ConverteMedidaPipe } from './converte-medida.pipe';

describe('ConverteMedidaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverteMedidaPipe();
    expect(pipe).toBeTruthy();
  });
});
